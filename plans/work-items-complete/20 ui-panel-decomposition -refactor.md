# UiPanel Decomposition

**Type:** refactor
**Priority:** 20
**Raised by:** GPT (#6 bad — "758-line edit-conflict magnet")
**Status:** complete

## Problem

`libs/draxul-ui/src/ui_panel.cpp` is a ~758-line file that mixes:
- ImGui styling (fonts, colours, window padding)
- Dockspace and window layout
- Metric rendering (startup timing, grid dimensions, RPC stats)
- SDL-to-ImGui key translation
- Input forwarding to ImGui
- Panel visibility lifecycle management

This is workable today but is an edit-conflict magnet. Any two agents working on diagnostics metrics and input forwarding simultaneously will conflict on this file.

## Affected Files

- `libs/draxul-ui/src/ui_panel.cpp`
- `libs/draxul-ui/include/draxul/ui_panel.h`
- `libs/draxul-ui/CMakeLists.txt`

## Implementation Plan

- [x] Extract ImGui styling into `ui_panel_style.cpp/.h` (colour palette, font push/pop, padding constants). `UiPanel` calls `push_style()`/`pop_style()`.
- [x] Extract SDL-to-ImGui key translation into `sdl_imgui_input.cpp/.h` (free functions that take `SDL_Event` and call ImGui IO). This is also useful for the input modal-state test (work item 10).
- [x] Extract metric rendering into `ui_metrics_panel.cpp/.h` — a standalone `render_metrics(const DiagnosticsData&)` function that `UiPanel` calls. This allows metrics display to be tested without a full `UiPanel`.
- [x] Keep the dockspace and window composition in `ui_panel.cpp` (it is the legitimate orchestrator).
- [x] Update `CMakeLists.txt` to add new source files.
- [x] Run smoke test after splitting.

## Test Plan

- [ ] With `ui_metrics_panel` extracted: write a test that constructs `DiagnosticsData` and calls `render_metrics` with a headless ImGui context. Assert the rendered text contains expected metric values.
- [ ] With SDL-to-ImGui input extracted: test (work item 10) can use the same translation functions without a full `UiPanel`.
- [ ] All existing UI tests pass.

## Dependencies

- Coordinate with work item 10 (input-dispatcher modal state test) — the SDL-to-ImGui input extraction makes that test easier.
- Standalone otherwise.

---
*Work item generated by claude-sonnet-4-6 — 2026-03-20*
