---
# Duplicate Keybinding Detection in Config

## Summary
If a user configures two keybindings for the same key+modifier, the current code resolves them silently with no documented policy. A test asserting deterministic, documented resolution (first wins / last wins / error) prevents confusing user-reported bugs.

## Context
`libs/draxul-app-support/src/app_config.cpp` and `tests/app_config_tests.cpp`. Duplicate keybindings are a realistic user mistake (copy-paste error in config.toml) and the resolution policy needs to be both predictable and tested.

## Steps
- [x] Read how keybindings are loaded from TOML
- [x] Write a config with two bindings for the same key+modifier
- [x] Assert: a warning is logged
- [x] Assert: a specific documented policy (first-wins or last-wins) is applied consistently
- [x] Add a comment to `app_config.cpp` documenting the policy
- [x] Register in existing `app_config_tests.cpp` or new file

## Acceptance Criteria
- [x] Duplicate binding produces a warning
- [x] Policy is documented and tested

## Notes
None.

*Work item generated by claude-sonnet-4-6*
