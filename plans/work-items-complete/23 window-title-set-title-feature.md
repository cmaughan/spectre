---
# Window Title from Neovim set_title Event

## Summary
Neovim sends `set_title` RPC events. Draxul should forward this to `SDL_SetWindowTitle()` so the OS taskbar, Alt-Tab switcher, and window manager show the current file path.

## Context
`UiEventHandler` in `libs/draxul-nvim/src/ui_events.cpp`. `IWindow` in `libs/draxul-window/`. Look for `set_title` handling (or its absence). This is a standard Neovim UI event sent whenever the active buffer name changes.

## Steps
- [x] Search `ui_events.cpp` for `set_title` — add handler if missing
- [x] Add `set_title(std::string_view title)` method to `IWindow` interface if not present
- [x] Implement in `SdlWindow` using `SDL_SetWindowTitle()`
- [x] Wire: `UiEventHandler::on_set_title()` → `IWindow::set_title()`
- [x] Add a test using a fake window that records the title string

## Acceptance Criteria
- [x] Opening a file in Neovim updates the window title
- [x] Test verifies `set_title` event routes to window

## Notes
All steps were already fully implemented prior to this work item being processed:
- `handle_set_title` in `ui_events.cpp` parses args and calls `on_title` callback
- `on_title` in `nvim_ui.h` is wired in `nvim_host.cpp` → `callbacks().set_window_title`
- `callbacks.set_window_title` in `app.cpp` calls `window_.set_title(title)`
- `SdlWindow::set_title()` calls `SDL_SetWindowTitle()`
- Test "ui event handler forwards title updates" exists in `tests/ui_events_tests.cpp`

*Work item generated by claude-sonnet-4-6*
