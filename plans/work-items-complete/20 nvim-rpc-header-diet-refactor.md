---
# nvim_rpc.h Public Header Diet

## Summary
`nvim_rpc.h` exposes threading primitives, `mpack.h`, and platform OS headers as part of its public API. This increases compile coupling across everything that includes it and makes boundary-clean changes harder.

## Context
`libs/draxul-nvim/include/draxul/nvim_rpc.h`. Every translation unit that includes this header transitively pulls in `<windows.h>` or `<unistd.h>`, `mpack.h`, and `std::mutex`/`std::thread` — all of which are implementation details that do not need to be visible to callers.

## Steps
- [x] Read `nvim_rpc.h` and identify which includes are truly needed in the public API vs implementation details
- [x] Move threading primitives (`std::mutex`, `std::thread`, etc.) to a PIMPL struct in `rpc.cpp`
- [x] Forward-declare `mpack_reader_t` if needed in the header, include `mpack.h` only in the `.cpp`
- [x] Move platform OS headers (`<windows.h>`, `<unistd.h>`) out of the public header
- [x] Verify all compilation units that include `nvim_rpc.h` still compile
- [x] Check that compile times improve (optional: measure with `cmake --build --target draxul -- --time`)

## Acceptance Criteria
- [x] `nvim_rpc.h` includes only `draxul/` types and standard library types
- [x] No threading primitives or platform OS headers in the public header
- [x] All tests pass

## Notes
None.

*Work item generated by claude-sonnet-4-6*
