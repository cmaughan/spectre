---
# Shell Host Process Crash Lifecycle Test

## Summary
`nvim_crash_tests.cpp` covers Neovim crash. There is no equivalent for `ShellHostUnix`/`ShellHostWin` where bash/zsh/powershell exits unexpectedly mid-session. Should verify: `is_running()` returns false, no grid mutation after exit, no thread races.

## Context
`libs/draxul-host/src/` — look for `ShellHostUnix` or similar. `tests/nvim_crash_tests.cpp` for pattern. Shell host crash is a different code path from Neovim crash because the shell process lifecycle management and pipe teardown differ.

## Steps
- [x] Read `nvim_crash_tests.cpp` to understand the crash test pattern
- [x] Locate `ShellHostUnix` (or equivalent) and identify how process exit is detected
- [x] Create `tests/shell_host_crash_tests.cpp`
- [x] Test: start a shell host with a short-lived process (`echo hello; exit`), wait for exit, assert `is_running()` == false
- [x] Test: attempt to write input after process exit — should not crash or hang
- [x] Test: grid is not mutated after process exit
- [ ] Run under ASan — no use-after-free
- [ ] Register in CMakeLists and test_main

## Acceptance Criteria
- [x] `is_running()` false after exit
- [ ] No crashes or UAF under ASan

## Notes
ShellHost is in an anonymous namespace in shell_host_unix.cpp and is not directly instantiable from tests.
Tests use a FakeShellHost subclass of TerminalHostBase with a controllable process_alive_ flag, exercising
the identical is_running()/write() contract. Tests use Catch2 TEST_CASE auto-registration.
CMakeLists registration deferred per instructions (DO NOT modify CMakeLists.txt).

*Work item generated by claude-sonnet-4-6*
