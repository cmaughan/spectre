# open_file_dialog Not Registered in Config

**Type:** bug
**Priority:** 01
**Raised by:** GPT (unique finding)
**Status:** open

## Problem

`app/gui_action_handler.cpp:50` recognises the `open_file_dialog` GUI action and handles it correctly at runtime. However `libs/draxul-app-support/src/app_config.cpp:26` does not include `"open_file_dialog"` in `kKnownGuiActions`, and `parse_gui_keybinding` (around line 454) rejects any keybinding that names an unknown action.

The result: the feature exists in code but cannot be bound in `config.toml`. Users who add `open_file_dialog = "Ctrl+O"` to their `[keybindings]` section see a parse error and the binding is silently dropped. This is a half-integrated feature.

## Affected Files

- `libs/draxul-app-support/src/app_config.cpp` — `kKnownGuiActions` array, `parse_gui_keybinding`
- `app/gui_action_handler.cpp` — action dispatch

## Implementation Plan

- [x] Add `"open_file_dialog"` to `kKnownGuiActions` in `app_config.cpp`.
- [x] Verify the string matches the action name used in `gui_action_handler.cpp` exactly (case-sensitive).
- [ ] Confirm that `AppConfig::save()` round-trips a keybinding for `open_file_dialog` correctly.
- [x] Check if any other actions handled in `gui_action_handler.cpp` are similarly missing from `kKnownGuiActions` and fix those too.

## Test Plan

- [ ] Add a unit test: parse a config containing `open_file_dialog = "Ctrl+O"`, assert no error and the binding is present in the parsed config.
- [ ] Config round-trip test: save a config with `open_file_dialog` binding, reload, assert binding survives. (This feeds into work item 05.)

## Dependencies

- Work item 05 (`open-file-path-special-chars-test`) exercises the full open-file flow; this fix is a prerequisite for the test to reach the handler via a config-bound key.

---
*Work item generated by claude-sonnet-4-6 — 2026-03-20*
