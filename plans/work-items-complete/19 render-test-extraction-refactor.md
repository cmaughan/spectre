---
# Extract Render Test Code from Production Binary

## Summary
`--render-test`, `show_render_test_window`, `render_target_pixel_width` etc. are compiled into the shipped binary. ~500 lines of production code paths exist solely for CI pixel-comparison. Should be gated behind a CMake option or extracted to a test-only target.

## Context
`app/app.cpp`, `app/app.h`, `AppOptions` struct. Look for `render_target` or `render_test` fields. Shipping render-test scaffolding in the production binary increases binary size and surface area, and makes the App class harder to reason about.

## Steps
- [x] Identify all render-test-related fields in `AppOptions` and code paths in `app.cpp`
- [x] Add a `DRAXUL_ENABLE_RENDER_TESTS` CMake option (default OFF in release, ON for test targets)
- [x] Wrap render-test code paths in `#ifdef DRAXUL_ENABLE_RENDER_TESTS`
- [x] Alternatively: extract the capture/compare logic into a `RenderTestCapture` class in `draxul-app-support` and only link it from test targets
- [x] Verify the production binary (`cmake --preset mac-release`) builds without render-test code
- [x] Verify render tests still pass in test builds

## Acceptance Criteria
- [x] Production binary does not contain render-test code paths
- [x] `draxul-tests` target still runs all render scenarios successfully

## Notes
Interdependency: this unblocks the App init/shutdown rollback integration test (see `06 app-init-shutdown-rollback-integration-test.md`). Do after Megacity removal.

*Work item generated by claude-sonnet-4-6*
