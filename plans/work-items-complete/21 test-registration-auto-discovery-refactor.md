---
# Test Registration Auto-Discovery

## Summary
`test_main.cpp` uses manual forward declarations and `run_*_tests()` calls. Forgetting a registration silently drops coverage. The project should use Catch2 `TEST_CASE`/`SECTION` auto-registration or add a manifest check.

## Context
`tests/test_main.cpp`, `tests/CMakeLists.txt`. Reference: the existing test files that use `run_*_tests()` pattern. The risk is that a new test file is written and compiled but never executed because the developer forgot to add a `run_*_tests()` call in `test_main.cpp`.

## Steps
- [x] Read `test_main.cpp` to understand the current registration pattern
- [x] Evaluate: switch to Catch2 `TEST_CASE` macros for new tests (auto-registered) while keeping existing tests working
- [x] OR: add a compile-time manifest check that lists all expected `run_*_tests` function names and fails to compile if any are missing
- [x] Migrate at least 3 existing test files to the Catch2 `TEST_CASE` pattern as a proof of concept
- [x] Update `CMakeLists.txt` to link Catch2 if not already present

## Acceptance Criteria
- [x] Adding a new `TEST_CASE` test file does not require touching `test_main.cpp`
- [x] Existing tests continue to pass

## Notes
New test files (05–16 above) should use whichever pattern is adopted here. Coordinate timing.

*Work item generated by claude-sonnet-4-6*
