# TerminalHostBase Further Decomposition

**Type:** refactor
**Priority:** 17
**Raised by:** GPT (#2 bad architecture), Gemini (#3 bad), Claude (agrees)
**Status:** complete

## Problem

`libs/draxul-host/include/draxul/terminal_host_base.h` is the inheritance root for Neovim, shell, and PowerShell hosts. The header is 137 lines; the implementation is 453 lines across two files.

**Already done:** All major subsystems are now extracted as separate classes held by value — `AltScreenManager`, `ScrollbackBuffer`, `SelectionManager`, `MouseReporter`, `VtState`, `VtParser`. The CSI dispatch lives in `terminal_host_base_csi.cpp`.

**Still remaining:** The header still owns all these subsystems regardless of host type. A shell/PowerShell host (local terminal) and the Neovim host share the same base class even though Neovim does not use scrollback, selection, or the VT state machine. The split into `LocalTerminalHost` (for PTY/shell) and a leaner `TerminalHostBase` (for Neovim) has not happened.

The practical consequence: shell crash and scrollback resize tests still require constructing a full `TerminalHostBase` with all subsystems live.

## Affected Files

- `libs/draxul-host/include/draxul/terminal_host_base.h`
- `libs/draxul-host/src/terminal_host_base.cpp`
- `libs/draxul-host/src/terminal_host_base_csi.cpp`
- All derived classes: `nvim_host`, `shell_host`, `powershell_host` (or equivalent)

## Implementation Plan

- [x] Map the current member variable list in `terminal_host_base.h` into categories: (a) always needed by all hosts, (b) needed only by terminal hosts (not Neovim), (c) needed only by local-process hosts, (d) needed only by Neovim host.
- [x] Extract category (b) and (c) into an intermediate `LocalTerminalHost : TerminalHostBase` that the shell/PowerShell hosts derive from, leaving only category (a) in `TerminalHostBase`.
- [ ] Consider splitting the scrollback + selection pair into a `ScrollingTerminalMixin` that can be composed rather than inherited. *(deferred — not required for this item)*
- [ ] Move `terminal_host_base_csi.cpp` logic into a free-function or standalone `CsiProcessor` class that `TerminalHostBase` holds by value (composition over inheritance for CSI dispatch). *(deferred — separate work item)*
- [x] After decomposition: re-run all tests to confirm no regressions. The goal is that `TerminalHostBase` construction in tests does not require all subsystems to be initialised.
- [x] Update `CMakeLists.txt` if new source files are added.

## Test Plan

- [x] All existing host tests pass after the refactor. (86 `[terminal]` tests pass.)
- [x] New `terminal_mouse_tests.cpp` and `selection_truncation_tests.cpp` test suites written and passing.

**Root cause fixed during testing:** Both test files defined `class TestTerminalHost` at global scope, causing an ODR violation where the linker silently merged the two different class layouts. Fixed by wrapping each `TestTerminalHost` in an anonymous namespace.

## Dependencies

- **Blocks** (downstream): shell crash lifecycle test, scrollback resize test.
- Work item 14 (megacity removal) is complete — no blocker there.

## Sub-Agent Suitability

This is a good multi-agent task: one agent can extract `LocalTerminalHost`, another can extract `CsiProcessor`. They operate on different files and can be merged. Assign in sequence (LocalTerminalHost first, then CsiProcessor) to avoid rebase conflicts on `terminal_host_base.h`.

---
*Work item generated by claude-sonnet-4-6 — 2026-03-20*
*Completed by claude-sonnet-4-6 — 2026-03-21*
