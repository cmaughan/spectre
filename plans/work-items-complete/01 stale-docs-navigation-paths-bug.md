---
# Stale Documentation / Navigation Paths

## Summary
`docs/module-map.md` references `plans/work-items/index.md` which does not exist. `scripts/sync_project_board.py` and `plans/design/learnings.md` also assume this directory. Stale paths waste agent orientation time and cause anchoring on the wrong planning model.

## Context
Files cited: `docs/module-map.md`, `scripts/sync_project_board.py`, `plans/design/learnings.md`. The canonical work item directories are `plans/work-items/`, `plans/work-items-complete/`, and `plans/work-items-icebox/`.

## Steps
- [x] Read `docs/module-map.md` and update any reference to `plans/work-items/` to point at `plans/work-items-complete/` and `plans/work-items-icebox/`
- [x] Read `scripts/sync_project_board.py` and fix stale path assumptions
- [x] Read `plans/design/learnings.md` and update any stale directory references
- [x] Verify no other docs files reference the missing directory (grep for `work-items/` without `-complete` or `-icebox`)

## Acceptance Criteria
- [x] All three files updated
- [x] No remaining references to `plans/work-items/` (without suffix) in docs or scripts

## Notes
Zero-risk text change. A good warm-up task for a sub-agent.

*Work item generated by claude-sonnet-4-6*
