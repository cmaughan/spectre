---
# UiPanel Keyboard and Mouse Capture Tests

## Summary
`UiPanel` has a large SDL-to-ImGui scancode mapping table that is hand-maintained and minimally tested. Keyboard and mouse capture state transitions are barely covered.

## Context
`libs/draxul-ui/src/ui_panel.cpp` — the scancode mapping table. `tests/ui_panel_layout_tests.cpp` — existing layout tests. The scancode table maps SDL scancodes to ImGui key identifiers; any missing or incorrect mapping silently breaks keyboard input for the affected key within panels.

## Steps
- [x] Read `ui_panel.cpp` and identify the scancode mapping table and capture state logic
- [x] Create `tests/ui_panel_input_tests.cpp`
- [x] Test: scancode table is complete — every SDL scancode in the table maps to a non-zero ImGui key
- [ ] Test: `UiPanel` captures keyboard when it reports `wants_keyboard_input()`
- [ ] Test: `UiPanel` releases keyboard when panel is dismissed/minimised
- [ ] Test: mouse events are routed correctly based on cursor position
- [x] Register in CMakeLists and test_main

## Acceptance Criteria
- [x] Scancode table completeness verified
- [ ] Capture state transitions tested

## Notes
`sdl_scancode_to_imgui_key` was moved out of the anonymous namespace (kept in `draxul` namespace) to allow extern declaration in tests.

*Work item generated by claude-sonnet-4-6*
