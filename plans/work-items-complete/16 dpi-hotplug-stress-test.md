---
# DPI Hotplug Rapid-Succession Stress Test

## Summary
`dpi_scaling_tests.cpp` tests a single DPI change. There is no test for rapid successive DPI changes (dragging window between displays), which is the real-world trigger for the `03 dynamic-dpi-hotplug -bug.md` class of bug.

## Context
`tests/dpi_scaling_tests.cpp` for existing pattern. `DisplayScaleChangedEvent` in types. The bug class manifests when the font pipeline and renderer receive rapid resize events faster than they can process — e.g., a pending atlas rebuild is interrupted by another scale change.

## Steps
- [x] Read `dpi_scaling_tests.cpp` to understand existing coverage
- [x] Create or extend with a stress test that fires 20 `DisplayScaleChangedEvent`s in a tight loop
- [x] After all events, assert: text service and renderer are in a consistent state
- [x] Assert: no double-free, no use-after-free under ASan
- [x] Assert: final DPI state matches the last event

## Acceptance Criteria
- [x] 20 rapid DPI events handled without crash
- [x] Final state consistent with last event

## Notes
None.

*Work item generated by claude-sonnet-4-6*
