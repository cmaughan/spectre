---
# TerminalHostBase Decomposition

## Summary
`terminal_host_base.cpp` (1000+ lines) owns VT parsing, cursor state, SGR, alt-screen, mouse reporting, selection, scrollback, and process I/O contracts. Too many concerns to be easily tested or modified in isolation.

## Context
`libs/draxul-host/src/terminal_host_base.cpp` and `libs/draxul-host/include/draxul/terminal_host_base.h`. The monolithic file makes it difficult to write targeted unit tests (e.g., cursor movement without spawning a process) and means any change risks unintended interactions between concerns.

## Steps
- [x] Read `terminal_host_base.h` and catalogue all private member state
- [x] Extract cursor + scroll region + mode flags into a `VtState` value struct (no virtual methods, POD-like)
- [x] Extract mouse reporting state and logic into a `MouseReporter` helper class (already existed)
- [x] Extract alt-screen management into an `AltScreenManager` helper (already existed)
- [x] Extract VT sequence handlers (CSI/ESC/control/OSC) into `terminal_host_base_csi.cpp`
- [x] Extract SGR/colour logic into `terminal_sgr.cpp` + `terminal_sgr.h` (free function `apply_sgr`)
- [x] Extract key encoding into `terminal_key_encoder.cpp` + `terminal_key_encoder.h` (free function `encode_terminal_key`)
- [x] `TerminalHostBase` delegates to these helpers; its own state shrinks significantly
- [x] Verify all existing tests still pass

## Acceptance Criteria
- [x] `terminal_host_base.cpp` is under 600 lines (now 503 lines)
- [x] `VtState`, `MouseReporter`, `AltScreenManager` are independently constructible and testable
- [x] All existing host tests pass

## Notes
Sub-agent friendly for the extraction step. Unblocks shell-host crash test and scrollback viewport resize test. Do after Megacity removal to reduce simultaneous churn.

*Work item generated by claude-sonnet-4-6*
