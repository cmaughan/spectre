---
# Config Round-Trip With Unknown / Future Keys

## Summary
No test writes a config with an unknown key and verifies: (a) loads without crash, (b) emits exactly one warning, (c) unknown key is not silently discarded from a subsequent `save()`. Guards against config-upgrade breakage.

## Context
`libs/draxul-app-support/src/app_config.cpp`. `tests/app_config_tests.cpp` for existing pattern. Config upgrade compatibility is important: a user who downgrades Draxul should not silently lose future config keys.

## Steps
- [x] Read `app_config_tests.cpp` to understand the test pattern
- [x] Write a TOML string with a known-good key and an unknown key (`future_option = true`)
- [x] Parse it and assert: no crash, warning logged
- [x] Save it back to a string and assert: known keys preserved, unknown key handling is documented (either preserved or dropped, but consistently)
- [x] Test with a TOML file that has an unknown section (`[future_section]`)

## Acceptance Criteria
- [x] Unknown key load does not crash
- [x] Exactly one warning emitted per unknown key
- [x] Documented (and tested) policy for save() of unknown keys

## Notes
None.

*Work item generated by claude-sonnet-4-6*
