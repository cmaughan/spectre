---
# CI Pipeline Definition (.github/workflows)

## Summary
No CI pipeline is checked in. The test suite (including sanitiser builds, render tests, smoke tests) can only be run manually. Automated enforcement on push prevents regressions.

## Context
`do.py`, `t.bat`, `CLAUDE.md` for the validation expectations. The existing CMake presets for mac-asan, mac-debug, etc. A CI pipeline ensures every PR is validated against the full test matrix before merge.

## Steps
- [ ] Create `.github/workflows/ci.yml`
- [ ] Job 1 (macOS): `cmake --preset mac-asan`, build `draxul draxul-tests`, `ctest -R draxul-tests`, `py do.py smoke`
- [ ] Job 2 (macOS release): `cmake --preset mac-release`, build `draxul`, smoke test
- [ ] Job 3 (Windows, if runner available): `cmake --preset default`, build `draxul draxul-tests`, ctest
- [ ] Add shader compilation verification step: assert all `.metal` and `.glsl` files compile
- [ ] Add a status badge to `README.md` (if it exists) or `CLAUDE.md`
- [ ] Ensure render-test scenarios are validated in CI using `DRAXUL_ENABLE_RENDER_TESTS=ON`

## Acceptance Criteria
- [ ] CI runs on every push and PR to main
- [ ] ASan build included
- [ ] Render test scenarios verified

## Notes
Interdependency: requires `19 render-test-extraction-refactor.md` to be done first so render-test targets are clean. Sub-agent friendly for authoring the YAML.

*Work item generated by claude-sonnet-4-6*
