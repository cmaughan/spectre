---
# VT Parser Fuzz Test with Corpus

## Summary
The VT parser (`vt_parser.cpp`) processes arbitrary bytes from a PTY with no fuzz coverage. Injecting random byte sequences should never crash, assert, or produce an inconsistent grid state. A fuzz harness analogous to `mpack_fuzz_tests.cpp` is needed.

## Context
`libs/draxul-host/src/vt_parser.cpp` (or wherever the VT parser lives). Reference: `tests/mpack_fuzz_tests.cpp` for harness pattern. The parser processes escape sequences, CSI, OSC, DCS passthrough, and raw UTF-8 bytes from a PTY; any of these paths can see malformed or truncated input.

## Steps
- [x] Read `mpack_fuzz_tests.cpp` to understand the existing fuzz harness pattern
- [x] Create `tests/vt_parser_fuzz_tests.cpp`
- [x] Feed random byte sequences to `VtParser::feed()` (Catch2 TEST_CASE harness)
- [x] Assert: no crash, no assertion failure, no inconsistent state after each call
- [x] Add a small corpus of known-tricky sequences: ESC alone, partial CSI, overlong OSC, null bytes in sequence
- [ ] Register the test in `tests/CMakeLists.txt` and `tests/test_main.cpp`

## Acceptance Criteria
- [x] Fuzz test compiles and runs under ASan
- [x] At least 10,000 random inputs processed without crash
- [x] Corpus of tricky sequences all handled gracefully

## Notes
Run this under the ASan preset (`cmake --preset mac-asan`). Sub-agent friendly: give the agent the mpack fuzz test as a reference.

CMakeLists.txt registration will be done separately.

*Work item generated by claude-sonnet-4-6*
