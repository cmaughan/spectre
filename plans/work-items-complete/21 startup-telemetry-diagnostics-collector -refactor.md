# 21 startup-telemetry-diagnostics-collector -refactor

**Priority:** LOW
**Type:** Refactor
**Raised by:** Claude (worst features #9)
**Model:** claude-sonnet-4-6

---

## Problem

`App` accumulates `StartupStep` timing entries during `initialize()` and stores them in a member `std::vector<StartupStep> startup_steps_`. These are only used by the diagnostics panel. This:

- Adds a telemetry concern to `App` that has nothing to do with orchestration.
- Makes `App` own data that `UiPanel`/diagnostics is the real consumer of.
- Couples the diagnostic panel to `App` directly rather than to an injectable data source.

---

## Steps

- [ ] Read `app/app.h` and `app/app.cpp` to find `StartupStep`, `startup_steps_`, and all places they are written/read.
- [ ] Read `libs/draxul-ui/src/ui_panel.cpp` to see how the panel reads startup timing data.
- [ ] Create a `DiagnosticsCollector` class (small header in `libs/draxul-ui/` or a new `libs/draxul-diagnostics/`):
  - `record_startup_step(std::string name, duration_t elapsed)`.
  - `startup_steps() const -> span<StartupStep>`.
- [ ] Thread a `DiagnosticsCollector&` or `shared_ptr<DiagnosticsCollector>` through `App::initialize()` calls.
- [ ] Remove `startup_steps_` from `App`. Replace all writes with `diagnostics_collector_.record_startup_step(...)`.
- [ ] Update `UiPanel` to hold a `shared_ptr<DiagnosticsCollector>` instead of reading from `App&`.
- [ ] Run `cmake --build build --target draxul draxul-tests`.
- [ ] Verify diagnostics panel still shows startup timing in the running app.
- [ ] Run `clang-format` on touched files.

---

## Acceptance Criteria

- [ ] `App` has no `StartupStep` or timing telemetry member variables.
- [ ] `DiagnosticsCollector` is the single owner of startup timing data.
- [ ] Diagnostics panel displays startup timing correctly.

---

## Notes

This is a low-risk, isolated refactor. Good candidate for a focused sub-agent.

*Work item generated by claude-sonnet-4-6*
