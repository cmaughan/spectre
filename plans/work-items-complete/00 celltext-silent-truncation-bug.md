---
# CellText Silent Truncation at 32 Bytes

## Summary
`CellText` stores UTF-8 clusters inline with a hard 32-byte max. Clusters that exceed this (complex emoji with multiple variation selectors) are silently truncated with no log warning and no test for the over-length path.

## Context
Look for `CellText` struct in `libs/draxul-grid/` or `libs/draxul-types/`. The silent truncation is the issue. When a complex Unicode cluster (e.g., family emoji with skin tone modifiers and ZWJ sequences) exceeds 32 bytes, bytes beyond the limit are dropped with no diagnostic output.

## Steps
- [x] Locate `CellText` definition and find where truncation occurs
- [x] Add a log warning (use existing log infrastructure) when a cluster is truncated
- [x] Add a unit test that constructs a >32-byte UTF-8 cluster and asserts the warning fires
- [x] Optionally: add a `// TODO: consider std::string for >32-byte clusters` comment

## Acceptance Criteria
- [x] Truncation emits a log warning
- [x] Test for over-length path passes

## Notes
Simple, zero-risk fix. Do NOT change the storage type (that is a larger refactor). Just add observability.

*Work item generated by claude-sonnet-4-6*
