---
# Scrollback Viewport Correctness During Grid Resize

## Summary
When the grid is resized (cols change), stored scrollback lines of the old width must be either re-wrapped or preserved as-is. No test asserts which behaviour is implemented, meaning a future refactor could silently change layout.

## Context
`libs/draxul-host/src/` — scrollback buffer. `tests/` for existing scrollback tests. The current policy (re-wrap vs preserve) is undocumented; this test pins the behaviour so any change is visible in the test diff.

## Steps
- [x] Read the scrollback buffer implementation to determine its current resize policy (re-wrap vs preserve)
- [x] Create `tests/scrollback_viewport_resize_tests.cpp`
- [x] Test: fill scrollback with lines, resize grid width narrower — assert lines are either re-wrapped or clamped (document which)
- [x] Test: resize grid width wider — assert lines are padded correctly
- [x] Test: scrollback viewport offset is clamped to valid range after resize
- [ ] Register in CMakeLists and test_main

## Acceptance Criteria
- [x] Policy is documented in test name/comment
- [x] Tests fail if policy changes silently

## Notes
Policy determined: resize() DISCARDS all content (clean break, not re-wrap). The implementation
resets count_, write_head_, offset_, and live_snapshot_ on every resize() call when cols differ.
Tests pin this behaviour with 5 TEST_CASEs using Catch2 auto-registration.
CMakeLists registration deferred per instructions (DO NOT modify CMakeLists.txt).

*Work item generated by claude-sonnet-4-6*
