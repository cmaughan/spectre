# 17 irenderer-interface-cleanup -refactor

**Priority:** HIGH
**Type:** Refactor
**Raised by:** Claude (bad thing #3)
**Model:** claude-sonnet-4-6

---

## Problem

`IRenderer` (in `libs/draxul-renderer/include/draxul/renderer.h`) inherits from `IGridRenderer`, `IImGuiHost`, and `ICaptureRenderer` — a multiple inheritance compatibility shim. The comment in the file explicitly calls this "backward-compat combined interface." `IImGuiHost` lifecycle concerns (frame begin/end, panel rendering) have no business on a grid renderer interface.

Downstream effects:
- `FakeRenderer` in tests must implement the full combined interface.
- Code that only needs `IGridRenderer` receives `IImGuiHost` methods as a "bonus".
- Adding a new concern (e.g., `ISnapshotRenderer`) widens the combined type for all callers.

---

## Steps

- [ ] Read `libs/draxul-renderer/include/draxul/renderer.h` in full.
- [ ] Read `libs/draxul-renderer/include/draxul/base_renderer.h` for the base hierarchy.
- [ ] Read `app/app.cpp` and `tests/fake_renderer.h` (or equivalent) to understand all `IRenderer` use sites.
- [ ] Evaluate the split:
  - Keep `IGridRenderer` as the grid-rendering contract.
  - Move `IImGuiHost` to `libs/draxul-ui/` or a new `draxul-imgui-host` interface.
  - Move `ICaptureRenderer` capture capability to a separate interface (see also item `20`).
  - Replace `IRenderer` with `RendererBundle` (which already exists) as the at-app-level composed type.
- [ ] Update `FakeRenderer` to implement only `IGridRenderer` (and optionally `IImGuiHost` separately).
- [ ] Update all call sites that use `IRenderer*` to use the appropriate narrower interface or `RendererBundle`.
- [ ] Run `cmake --build build --target draxul draxul-tests`.
- [ ] Run `clang-format` on touched files.

---

## Acceptance Criteria

- [ ] `IRenderer` multiple inheritance shim is removed or replaced by `RendererBundle` at all call sites.
- [ ] `FakeRenderer` no longer inherits the combined interface.
- [ ] All tests pass.

---

## Interdependencies

- Should be done **before** `20 render-test-capture-separation -refactor` — both touch the renderer interface; doing them in sequence with the same agent avoids conflicts.
- Related to `01 gridhostbase-raw-ptr-safety -bug` — both touch the GridHostBase neighbourhood.

*Work item generated by claude-sonnet-4-6*
