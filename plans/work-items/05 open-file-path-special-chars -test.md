# Open-File Path Special Characters Test

**Type:** test
**Priority:** 05
**Raised by:** GPT (primary)
**Status:** open

## Problem / Gap

There is no integration test that exercises the full path: file dropped / dialog opened → `InputDispatcher` → `NvimHost` → `nvim_command`. Paths with spaces, quotes, Unicode characters, and shell-metacharacter sequences are untested. The bug (work item 00) is silent — the wrong file is opened with no error.

## Affected Files

- `app/input_dispatcher.cpp`
- `libs/draxul-host/src/nvim_host.cpp`
- `tests/` — new test file

## Test Plan

- [ ] Create `tests/open_file_tests.cpp` using `FakeRenderer` + `FakeWindow` + a real (or fake) Neovim host stub that records the command string passed to `nvim_command`.
- [ ] Test case: path with ASCII spaces — assert the command contains a properly escaped/quoted path.
- [ ] Test case: path with double quotes (`"`) — assert no command injection.
- [ ] Test case: path with pipe `|` — assert no second command is injected.
- [ ] Test case: path with percent `%` — assert no Vimscript expansion.
- [ ] Test case: path with Unicode characters (e.g. `café/résumé.txt`) — assert correct encoding.
- [ ] Test case: config binding `open_file_dialog = "Ctrl+O"` — assert binding is parsed and dispatches correctly (exercises work item 01 fix).
- [ ] If a sub-agent is used for this item, also have it verify the `fnameescape` or equivalent helper is called at the right layer.

## Implementation Notes

- Use the existing `replay_fixture.h` pattern as a model for test harness setup.
- The test should **not** launch a real Neovim process; use the NvimHost's injectable command interface or a fake host.

## Dependencies

- **Depends on:** work item 00 (path injection fix) and work item 01 (config registration fix) — tests should be written to verify the fixes.
- **Depends on:** work item 04 (AppConfig SDL decoupling) for config round-trip portion.

---
*Work item generated by claude-sonnet-4-6 — 2026-03-20*
