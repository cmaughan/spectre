# 09 ligature-expand-bounds -test

**Priority:** MEDIUM
**Type:** Test
**Raised by:** Claude (best tests #1)
**Model:** claude-sonnet-4-6

---

## Problem

`GridRenderingPipeline::expand_dirty_cells_for_ligatures` has no direct unit tests. An off-by-one in the neighbour-expansion logic (expanding past col 0 or past `grid.cols()-1`) causes silent rendering artefacts — adjacent cells are incorrectly included or missed without any assertion failure.

---

## Steps

- [ ] Read `libs/draxul-app-support/src/grid_rendering_pipeline.cpp`. Find `expand_dirty_cells_for_ligatures`.
- [ ] Understand the expansion contract: a dirty cell at column `c` should expand to include the ligature neighbour cells. Identify the maximum expansion radius used.
- [ ] Add test cases (new or in existing grid pipeline test file):
  1. Dirty cell at col 0 → expanded set does not include col -1 (no underflow).
  2. Dirty cell at last col (`grid.cols()-1`) → expanded set does not include `grid.cols()` (no overflow).
  3. Single-cell grid (1×1) → expansion of the only cell does not crash.
  4. Two adjacent dirty cells → expansion does not double-count.
  5. Dirty cell in the middle of the grid → expansion includes the expected neighbours on both sides.
- [ ] Run `cmake --build build --target draxul-tests && ctest`.
- [ ] Run `clang-format` on touched files.

---

## Acceptance Criteria

- [ ] All 5 test cases pass.
- [ ] No out-of-bounds cell indices in expansion results.

---

## Interdependencies

- Write this test after `02 ligature-expand-hotpath-alloc -bug` is fixed, so the test exercises the post-fix code path.

*Work item generated by claude-sonnet-4-6*
