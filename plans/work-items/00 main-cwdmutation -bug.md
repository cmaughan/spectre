# 00 main-cwdmutation -bug

**Priority:** HIGH
**Type:** Bug
**Raised by:** GPT (High severity)
**Model:** claude-sonnet-4-6

---

## Problem

`app/main.cpp` calls `SetCurrentDirectory` (Windows) or `chdir` (macOS) to change the process working directory to the executable's directory at startup. This is a global side effect that:

- Makes config file lookup (`config.toml`) depend on how the app was launched (double-click vs CLI vs test runner), not on an explicit path.
- Leaks into child-process spawning (`nvim --embed`) — relative paths passed to `CreateProcess`/`execvp` are resolved against the new CWD, not the user's shell CWD.
- Contaminates test runs that share a process with the app layer.
- Makes future agent-launched sessions non-deterministic about which directory is "home".

---

## Steps

- [ ] Read `app/main.cpp` in full. Find the CWD mutation call and understand why it was added.
- [ ] Read the config loading code in `libs/draxul-app-support/` to see where `config.toml` is located. Determine if the CWD mutation was compensating for a relative path assumption there.
- [ ] Replace the CWD mutation with explicit absolute path derivation:
  - On macOS: use `_NSGetExecutablePath` or `realpath(argv[0])` to get the executable dir, then pass it explicitly to `AppConfig::load()` / `AppConfig::config_path()`.
  - On Windows: use `GetModuleFileName(NULL, ...)` to get the exe path, derive the dir, pass explicitly.
- [ ] Remove the `chdir`/`SetCurrentDirectory` call entirely.
- [ ] Verify that `nvim --embed` subprocess launch still resolves the `nvim` binary correctly (it uses `PATH`, not CWD — should be fine).
- [ ] Build and run `cmake --build build --target draxul draxul-tests`.
- [ ] Run smoke test: `py do.py smoke`.
- [ ] Run `clang-format` on all touched files.

---

## Acceptance Criteria

- [ ] No `chdir` / `SetCurrentDirectory` call in `main.cpp` or any startup path.
- [ ] `config.toml` is still found correctly when the app is launched via double-click, CLI from a different directory, and from the build directory.
- [ ] `nvim` subprocess launches successfully.
- [ ] All tests pass.

---

## Notes

If the CWD mutation was needed because some asset path (fonts, shaders) was relative, those paths must be made absolute at the point of use using the executable directory — not by mutating global process state. Check `fonts/` and `shaders/` path resolution as part of the fix.

---

## Interdependencies

- `12 cli-args-cwd-behavior -test` should be written after this fix to lock in the correct behaviour.

*Work item generated by claude-sonnet-4-6*
