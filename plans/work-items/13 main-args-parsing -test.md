# main.cpp Argument Parsing Tests

**Type:** test
**Priority:** 13
**Raised by:** GPT
**Status:** open

## Problem / Gap

`app/main.cpp` parses command-line arguments for flags like `--console`, `--render-test`, host selection, and config path override. These paths are currently untested. A regression in argument parsing (e.g., a new flag shadowing an existing one, or a `--render-test` flag being silently ignored) would only be caught manually.

## Affected Files

- `app/main.cpp` — argument parsing
- `tests/` — new test file

## Test Plan

- [ ] Extract the argument-parsing logic from `main.cpp` into a standalone function `parse_args(int argc, char** argv) -> AppOptions` that can be called from tests without starting the app.
- [ ] Create `tests/main_args_tests.cpp`.
- [ ] Test: `--console` sets the console allocation flag.
- [ ] Test: `--render-test basic-view` sets render-test mode with the correct scenario name.
- [ ] Test: unknown flags are either rejected with a clear error or silently ignored (document which is intended).
- [ ] Test: `--config /path/to/config.toml` overrides the default config path.
- [ ] Test: host selection argument (e.g., `--host shell`) selects the correct host type.
- [ ] Test: empty arguments → defaults are applied correctly.
- [ ] Test: conflicting flags → deterministic winner or clear error.

## Implementation Notes

- The extraction of `parse_args` is a minimal refactor (a few lines). Include it as part of this test work item rather than filing a separate refactor item.
- Use Catch2 `SECTION` blocks for each flag combination.

## Dependencies

- Standalone. Minor refactor of `main.cpp` is self-contained within this item.

---
*Work item generated by claude-sonnet-4-6 — 2026-03-20*
