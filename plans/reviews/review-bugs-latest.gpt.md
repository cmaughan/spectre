**Findings**

1. `CRITICAL` [nvim_host.cpp:24](/Users/cmaughan/dev/Draxul/libs/draxul-host/src/nvim_host.cpp#L24), [host_manager.cpp:444](/Users/cmaughan/dev/Draxul/app/host_manager.cpp#L444), [rpc.cpp:58](/Users/cmaughan/dev/Draxul/libs/draxul-nvim/src/rpc.cpp#L58), [ui_request_worker.h:25](/Users/cmaughan/dev/Draxul/libs/draxul-runtime-support/include/draxul/ui_request_worker.h#L25), [nvim_process.cpp:43](/Users/cmaughan/dev/Draxul/libs/draxul-nvim/src/nvim_process.cpp#L43). `NvimHost::initialize_host()` starts the RPC reader thread and the resize worker before `attach_ui()` / `execute_startup_commands()` can fail. If either later step returns `false`, `HostManager::create_host_for_leaf()` destroys the partially initialized host without calling `shutdown()`. The implicit destruction path then hits joinable `std::thread` members in `NvimRpc` / `UiRequestWorker`, which causes `std::terminate`; the spawned `nvim` child is also left running because `NvimProcess` has a default destructor. Trigger: any startup-time failure after line 51, for example `nvim_ui_attach` rejection or a bad startup command. Suggested fix: add a rollback guard inside `NvimHost::initialize_host()` that calls `ui_request_worker_.stop(); rpc_.shutdown(); nvim_process_.shutdown();` on every failure after startup begins, or make those members fully RAII-safe on destruction.

2. `CRITICAL` [rpc.cpp:209](/Users/cmaughan/dev/Draxul/libs/draxul-nvim/src/rpc.cpp#L209), [rpc.cpp:237](/Users/cmaughan/dev/Draxul/libs/draxul-nvim/src/rpc.cpp#L237), [rpc.cpp:256](/Users/cmaughan/dev/Draxul/libs/draxul-nvim/src/rpc.cpp#L256), [rpc.cpp:287](/Users/cmaughan/dev/Draxul/libs/draxul-nvim/src/rpc.cpp#L287). The RPC dispatcher only checks top-level array length, then immediately calls `as_int()` / `as_str()` on fixed positions. A valid-msgpack but wrong-shaped packet such as `[1, "oops", nil, 0]` or `[2, 7, []]` throws `std::bad_variant_access` on the reader thread, and there is no catch boundary in `reader_thread_func()`, so the whole process aborts. Suggested fix: validate each field type before access, or wrap `dispatch_rpc_message()` in a `try/catch` that logs the malformed packet and marks the transport failed instead of letting the exception escape the thread.

3. `CRITICAL` [ui_events.cpp:298](/Users/cmaughan/dev/Draxul/libs/draxul-nvim/src/ui_events.cpp#L298), [ui_events.cpp:330](/Users/cmaughan/dev/Draxul/libs/draxul-nvim/src/ui_events.cpp#L330), [ui_events.cpp:357](/Users/cmaughan/dev/Draxul/libs/draxul-nvim/src/ui_events.cpp#L357), [ui_events.cpp:422](/Users/cmaughan/dev/Draxul/libs/draxul-nvim/src/ui_events.cpp#L422), [ui_events.cpp:473](/Users/cmaughan/dev/Draxul/libs/draxul-nvim/src/ui_events.cpp#L473). Multiple redraw handlers trust inner element types after only checking the outer array size. Examples: `handle_grid_resize()` assumes numeric cols/rows, `handle_mode_info_set()` assumes slot 1 is an array and map keys/values have the expected scalar types, and `handle_option_set()` / `handle_set_title()` assume strings. A malformed redraw batch like `["mode_info_set", [0, 123]]` or `["grid_resize", [1, "80", "24"]]` therefore throws on the main thread during normal redraw processing and terminates the UI. Suggested fix: replace direct `as_*()` calls in redraw handlers with typed helper accessors and drop malformed batches with a warning instead of throwing.

4. `HIGH` [bmp.cpp:46](/Users/cmaughan/dev/Draxul/libs/draxul-types/src/bmp.cpp#L46). `write_bmp_rgba()` unconditionally calls `std::filesystem::create_directories(path.parent_path())`. When the caller passes a bare filename like `out.bmp`, `parent_path()` is empty; standard library implementations commonly throw `filesystem_error` for that, and this path is not caught here. Trigger: `--screenshot out.bmp` or any other capture/export path without a directory component. Suggested fix: guard the call with `if (!path.parent_path().empty()) std::filesystem::create_directories(path.parent_path());`.

