# 05 grid-scroll-stress -test

## Summary

Add a stress / fuzz test for `Grid::scroll()` that exercises all out-of-bounds combinations
and verifies that no UB occurs, no cells are corrupted, and `dirty_marks_` stays consistent.
This is the guard that prevents regressions in the fix from `00 grid-scroll-bounds-safety`.

Flagged by **[Claude]** (test #1) and **[GPT]** (test #1).

---

## Prerequisites

- `00 grid-scroll-bounds-safety -bug` should be fixed first so the test starts green.
  The test can be written before the fix to watch it go red → green.

---

## Implementation Plan

- [ ] Create `tests/test_grid_scroll_stress.cpp`.
- [ ] Register it in `tests/CMakeLists.txt` and `tests/test_main.cpp` (or use auto-discovery
  if `12 test-auto-registration` is done first).
- [ ] Write parameterized cases:
  - `delta == 0` — no-op, grid unchanged.
  - `delta == region_height` — full clear, no cells copied.
  - `delta > region_height` — oversized, should not corrupt.
  - `delta < 0` (if API allows) — reverse scroll.
  - Random fuzz: 500 iterations of random (region, delta) pairs including extreme values.
- [ ] After each `scroll()` call:
  - Verify `dirty_marks_` bit count equals the number of actually-dirtied cells.
  - Verify no cell outside the scroll region was modified (save a snapshot of boundary
    cells before the call and compare after).
  - Verify `get_dirty_cells()` returns a consistent set.
- [ ] Run the test under AddressSanitizer:
  ```
  cmake --preset mac-asan && cmake --build build --target draxul-tests
  ctest --test-dir build -R grid_scroll_stress
  ```
- [ ] All cases must pass with zero ASan/UBSan reports.
- [ ] Run `clang-format` on new file.

## Acceptance Criteria

- [ ] `grid_scroll_stress` test exists and passes.
- [ ] Passes under ASan with no memory errors.
- [ ] Oversized-delta case does not corrupt out-of-region cells.
- [ ] `dirty_marks_` is consistent with `cells_` after every operation.

## Affected Files

- `tests/test_grid_scroll_stress.cpp` (new)
- `tests/CMakeLists.txt`
- `tests/test_main.cpp`

## Interdependencies

- Depends on `00 grid-scroll-bounds-safety` for the test to pass.
- Easier to add if `12 test-auto-registration` is done first (avoids dual-file edit).

---

*Generated by: claude-sonnet-4-6 (consensus review 2026-03-19)*
