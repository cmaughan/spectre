# 05 csi-cursor-boundary -test

**Priority:** HIGH
**Type:** Test
**Raised by:** Claude (best tests #7)
**Model:** claude-sonnet-4-6

---

## Problem

CSI cursor-movement sequences (`CUP`, `CUU`, `CUD`, `CUF`, `CUB`) at grid edges have no explicit unit tests. A cursor at row 0, col 0 receiving CUB (cursor back) should stay at (0,0) — not wrap or underflow. A cursor at the last row/col receiving CUD/CUF should clamp. These edge cases are easy to regress when modifying `TerminalHostBase` CSI handling.

---

## Steps

- [ ] Read `libs/draxul-host/src/terminal_host_base_csi.cpp` (or equivalent) to find CSI cursor movement dispatch.
- [ ] Read existing CSI tests in `tests/` (search for `CUP`, `CUU`, `CUD`).
- [ ] Add test cases using `replay_fixture.h` or a direct `TerminalHostBase` test harness:
  - `CUB` at col 0 → cursor stays at col 0.
  - `CUU` at row 0 → cursor stays at row 0.
  - `CUF` at last col → cursor clamps to last col.
  - `CUD` at last row → cursor clamps to last row.
  - `CUP row=0 col=0` (1-indexed: row=1 col=1) → positions at (0,0).
  - `CUP` with row/col beyond grid bounds → clamps to grid edges.
  - `CUP` with row=0 col=0 (0-indexed as received) — verify how out-of-range VT values are handled.
- [ ] Add these to a new or existing CSI test file.
- [ ] Run `cmake --build build --target draxul-tests && ctest`.
- [ ] Run `clang-format` on touched files.

---

## Acceptance Criteria

- [ ] At least 6 boundary test cases pass.
- [ ] No existing test is broken.

---

## Notes

Use `replay_fixture.h` builders where possible to avoid replicating msgpack construction by hand. A sub-agent can work on this in parallel with `06 rpc-fragmentation-pipe-boundary` since they touch different subsystems.

*Work item generated by claude-sonnet-4-6*
