---
# SDL Key Encoding Exhaustive Table Test

## Summary
`encode_key_tests.cpp` only covers a subset of key combinations. A table-driven test exhaustively encoding every `SDL_Keycode` × modifier combination would catch regressions when SDL3 adds or renames keycodes.

## Context
`tests/encode_key_tests.cpp`. The key encoding logic is in `libs/draxul-nvim/` or `libs/draxul-app-support/`. The encoder translates SDL keycodes and modifiers into Neovim key notation strings (e.g., `<C-a>`, `<S-F1>`).

## Steps
- [ ] Read `encode_key_tests.cpp` and identify what is currently covered
- [ ] Enumerate all `SDLK_*` values that the encoder is expected to handle
- [ ] For each: assert it produces a non-empty string or assert it is explicitly marked as intentionally unmapped
- [ ] Add table entries for modifier combinations (Ctrl, Shift, Alt, Meta) on printable keys
- [ ] Extend registration in CMakeLists if a new file is needed

## Acceptance Criteria
- [ ] All expected SDL keycodes covered in table-driven fashion
- [ ] Intentionally-unmapped keys are explicitly listed (not silently absent)

## Notes
None.

*Work item generated by claude-sonnet-4-6*
