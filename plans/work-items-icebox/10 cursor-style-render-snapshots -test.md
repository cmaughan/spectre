# 10 cursor-style-render-snapshots -test

## Summary

Add render snapshot tests for cursor style variants: block, vertical bar, underline, and
undercurl. Each should be blessed as a reference image and validated in CI so that shader
or cursor-rendering changes are caught visually.

Flagged by **[GPT]** (test #5). Agreed implicitly by **[Claude]** (guicursor support work).

---

## Implementation Plan

- [ ] Study the existing render snapshot framework in `tests/render_test.h/cpp` and
  `do.py bless*` workflow documented in `CLAUDE.md`.
- [ ] Create a new render scenario or extend an existing one in the test configuration
  that puts the cursor into each of the following shapes:
  - Block (normal mode)
  - Vertical bar (insert mode)
  - Underline (replace mode)
  - Undercurl (if supported)
- [ ] Ensure the test fixture drives the cursor to each state deterministically (e.g.,
  via injected `mode_change` RPC events or `guicursor` option setting).
- [ ] Run with `--render-test` flag and bless references:
  ```
  py do.py blessbasic   # or whichever scenario covers cursor variants
  ```
- [ ] Add CI step (or confirm existing `ctest` covers the new scenarios) to run
  `draxul-render-cursor-styles` or similar scenario name.
- [ ] Sub-agent may be appropriate here: one agent to write the scenario config, another
  to bless references on the target platform.
- [ ] Run `clang-format` on any C++ test additions.

## Acceptance Criteria

- [ ] Four cursor shape scenarios exist as named render test cases.
- [ ] Each has a blessed reference image checked into the repo.
- [ ] CI fails if any cursor shape regresses.

## Affected Files

- `tests/` (new scenario files or extensions to `render_test.cpp`)
- `do.py` (if new `bless*` targets needed)
- Reference image files (new, in `tests/` or `tests/references/`)

## Interdependencies

- `19 guicursor-full-support -feature` — once full guicursor is implemented, the
  reference images will need re-blessing. Plan for this.
- `13 shader-constants-unification -refactor` — shader constant changes may affect
  cursor rendering; re-bless after that refactor if needed.

---

*Generated by: claude-sonnet-4-6 (consensus review 2026-03-19)*
