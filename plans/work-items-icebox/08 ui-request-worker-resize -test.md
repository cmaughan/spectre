# 08 ui-request-worker-resize -test

## Summary

Add a test that fires many resize events at `UiRequestWorker` in rapid succession and
verifies that: only the last (or a coalesced) resize is dispatched to `nvim_ui_try_resize`,
not all N, and that the worker thread joins cleanly on `stop()`.

Flagged by **[Claude]** (test #6). Related to `17 ui-request-worker-resize-coalescing`.

---

## Prerequisites

- `17 ui-request-worker-resize-coalescing -refactor` ideally done first, but this test
  can be written against the current behaviour to document what "should" happen.

---

## Implementation Plan

- [ ] Locate `UiRequestWorker` — likely in `libs/draxul-app-support/` or `libs/draxul-nvim/`.
- [ ] Create `tests/test_ui_request_worker_resize.cpp`.
- [ ] Register in `tests/CMakeLists.txt` and `tests/test_main.cpp`.
- [ ] Use a mock RPC / callback counter to record how many `nvim_ui_try_resize` calls
  are actually emitted.
- [ ] Write test cases:
  1. **Single resize**: enqueue one resize event, assert exactly one RPC call with
     correct dimensions.
  2. **50 rapid resizes**: enqueue 50 consecutive resize events before the worker can
     drain. Assert ≤ 2 RPC calls (last wins or coalescence), not 50.
  3. **Clean shutdown**: after rapid resizes, call `stop()`. Assert the thread joins
     within 1 second and no RPC call is in-flight after join.
  4. **Stop while idle**: call `stop()` with no queued events. Assert clean join.
- [ ] Build and run: `ctest -R ui_request_worker_resize`.
- [ ] Run `clang-format`.

## Acceptance Criteria

- [ ] 50 rapid resizes result in ≤ 2 dispatched RPC calls.
- [ ] `stop()` always joins cleanly within 1 second.
- [ ] No use-after-free or data race (run under TSan if available).

## Affected Files

- `tests/test_ui_request_worker_resize.cpp` (new)
- `tests/CMakeLists.txt`, `tests/test_main.cpp`
- `UiRequestWorker` (may need RPC callback injection)

## Interdependencies

- This test validates `17 ui-request-worker-resize-coalescing`.
- Write alongside or just after item 17 for best effect.

---

*Generated by: claude-sonnet-4-6 (consensus review 2026-03-19)*
