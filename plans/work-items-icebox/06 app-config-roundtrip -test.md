# 06 app-config-roundtrip -test

## Summary

Add a roundtrip test for `AppConfig`: serialize to TOML → parse back → assert equality.
Additionally assert that unknown keys emit a warning and type mismatches produce an error,
not a silent default. Guards the fix from `02 config-unknown-key-warning`.

Flagged by **[Claude]** (test #3).

---

## Prerequisites

- `02 config-unknown-key-warning -bug` should be fixed first so the warning assertion
  is actually testable.

---

## Implementation Plan

- [ ] Create `tests/test_app_config_roundtrip.cpp`.
- [ ] Register in `tests/CMakeLists.txt` and `tests/test_main.cpp` (or auto-discovery).
- [ ] Write test cases:
  1. **Roundtrip**: construct `AppConfig` with non-default values, serialize to a TOML
     string in memory, parse it back, assert `original == parsed` field-by-field.
  2. **Unknown key**: parse a TOML snippet containing `font_szie = 14`. Assert that a
     warning is emitted (capture log output or use a test log sink). Assert that
     `AppConfig::font_size` remains at its default value.
  3. **Type mismatch**: parse `font_size = "big"`. Assert that an error is emitted and
     the field falls back to default rather than crashing.
  4. **Empty file**: parse an empty TOML string. Assert all fields are at defaults and
     no error is emitted.
- [ ] If `AppConfig` does not have a serialization path, implement a minimal `to_toml()`
  helper for test purposes only.
- [ ] Build and run: `cmake --build build --target draxul-tests && ctest -R app_config`.
- [ ] Run `clang-format`.

## Acceptance Criteria

- [ ] `app_config_roundtrip` test exists and passes.
- [ ] Roundtrip case: `original == parsed` for all fields.
- [ ] Unknown-key case: warning logged, field at default.
- [ ] Type-mismatch case: error logged, field at default, no crash.

## Affected Files

- `tests/test_app_config_roundtrip.cpp` (new)
- `tests/CMakeLists.txt`
- `tests/test_main.cpp`
- `app/app_config.h` / `app/app_config.cpp` (if `to_toml()` helper needed)

## Interdependencies

- Depends on `02 config-unknown-key-warning` for the warning assertions to work.
- Easier to add if `12 test-auto-registration` is done first.
- If `14 appconfig-appoptions-unification` is done first, only one struct to test.

---

*Generated by: claude-sonnet-4-6 (consensus review 2026-03-19)*
