---
# Agent Scripts Deduplication

## Summary
`ask_agent_gpt.py`, `ask_agent_claude.py`, and `ask_agent_gemini.py` duplicate `resolve_path`, prompt-file loading, prepend-file handling, and output handling logic. Will drift silently.

## Context
`scripts/ask_agent_gpt.py`, `scripts/ask_agent_claude.py`, `scripts/ask_agent_gemini.py`. The three scripts evolved in parallel and now share substantial boilerplate. Any fix to the shared logic (e.g., path resolution on Windows) must be applied three times.

## Steps
- [ ] Read all three scripts and catalogue duplicated functions/logic
- [ ] Create `scripts/agent_common.py` with shared: `resolve_path()`, `load_prompt_file()`, `load_prepend_file()`, `write_output()`, and any shared CLI argument handling
- [ ] Update each agent script to import from `agent_common`
- [ ] Verify all three scripts still function correctly (`python scripts/ask_agent_claude.py --help` etc.)

## Acceptance Criteria
- [ ] `agent_common.py` contains all shared logic
- [ ] Each agent script is under 30 lines (thin wrapper)
- [ ] Existing usage unchanged

## Notes
None.

*Work item generated by claude-sonnet-4-6*
