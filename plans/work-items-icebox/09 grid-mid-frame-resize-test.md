---
# Grid Resize Under Load (Mid-Frame)

## Summary
There is no test that constructs a grid, partially writes cells, fires a `grid_resize`, then calls `flush` — asserting the renderer received a correctly-sized cell array with no out-of-bounds writes.

## Context
`libs/draxul-grid/` for the Grid type. `tests/` for existing grid tests. Look at `grid_scroll_wide_char_test.md` in work-items-complete for pattern. Mid-frame resizes are the real-world trigger for OOB writes if the dirty tracking or cell array is not atomically resized.

## Steps
- [ ] Create `tests/grid_mid_frame_resize_tests.cpp`
- [ ] Test: write cells to a 10×5 grid, resize to 8×4, flush — renderer receives 8×4 cells
- [ ] Test: resize larger (10×5 → 12×6) — new cells are in default/blank state
- [ ] Test: partial write (only left half of grid) + resize — no OOB access
- [ ] Test: resize with wide-char cells at the boundary — wide cells are cleanly handled
- [ ] Register in CMakeLists and test_main

## Acceptance Criteria
- [ ] All scenarios pass under ASan (no OOB writes)
- [ ] Renderer receives correct cell count after resize

## Notes
None.

*Work item generated by claude-sonnet-4-6*
