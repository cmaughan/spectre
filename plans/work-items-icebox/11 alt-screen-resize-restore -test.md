# 11 alt-screen-resize-restore -test

## Summary

Add a test that enters the alternate screen (e.g., via a VT sequence), performs a resize,
returns to the primary screen, and verifies that: the primary screen scrollback is
intact, the grid dimensions are correct, and no cells have been corrupted.

Flagged by **[GPT]** (test #8).

---

## Implementation Plan

- [ ] Locate the VT / alt-screen handling in `TerminalHostBase` or the VT parser.
- [ ] Create `tests/test_alt_screen_resize_restore.cpp`.
- [ ] Register in `tests/CMakeLists.txt` and `tests/test_main.cpp`.
- [ ] Write a test using the existing VT-feed / replay fixture infrastructure:
  1. Feed primary-screen content to fill ~20 lines.
  2. Send `ESC[?1049h` (enter alt screen). Assert grid is cleared.
  3. Resize grid: call `Grid::resize()` or equivalent.
  4. Feed alt-screen content.
  5. Send `ESC[?1049l` (exit alt screen). Assert:
     - Primary screen content is restored.
     - Grid dimensions match the post-resize dimensions.
     - Scrollback is intact (line count unchanged).
     - No cell outside the visible area was corrupted.
- [ ] Build and run: `ctest -R alt_screen_resize_restore`.
- [ ] Run `clang-format`.

## Acceptance Criteria

- [ ] Test exists and passes.
- [ ] Primary screen scrollback is preserved across alt-screen cycle.
- [ ] Grid dimensions are correct after resize during alt-screen.
- [ ] No cell corruption.

## Affected Files

- `tests/test_alt_screen_resize_restore.cpp` (new)
- `tests/CMakeLists.txt`, `tests/test_main.cpp`

## Interdependencies

- `16 terminal-host-base-decomposition -refactor` — decomposing TerminalHostBase first
  makes it easier to test alt-screen in isolation. Can be written against the current
  class, but may need updates after 16.
- Easier with `12 test-auto-registration` done first.

---

*Generated by: claude-sonnet-4-6 (consensus review 2026-03-19)*
