# 07 cursor-blinker-dpi -test

## Summary

Add a test that verifies `CursorBlinker`'s timing state machine does not produce a spurious
off→on or freeze transition when `display_scale` changes mid-sequence. This guards DPI
migration behavior (e.g., moving the window between a Retina and non-Retina display).

Flagged by **[Claude]** (test #5). Agreed relevant by **[Gemini]** and **[GPT]** (multi-monitor
DPI hotplug is a high-priority stability test for both).

---

## Implementation Plan

- [ ] Locate `CursorBlinker` — likely in `libs/draxul-app-support/` or `app/`.
- [ ] Create `tests/test_cursor_blinker_dpi.cpp`.
- [ ] Register in `tests/CMakeLists.txt` and `tests/test_main.cpp`.
- [ ] Write test cases:
  1. **Baseline blink**: instantiate `CursorBlinker`, tick at 60 fps for 2 blink cycles,
     assert on/off transitions occur at expected tick counts.
  2. **DPI change mid-cycle**: after the cursor has been on for half a blink period,
     change `display_scale` from 1.0 to 2.0. Assert:
     - No spurious extra on→off or off→on transition occurs.
     - Blink timing after the DPI change is correct relative to the new scale.
  3. **DPI change at boundary**: change `display_scale` exactly at the transition tick.
     Assert no double-transition or frozen-off state.
- [ ] Use a mock or injectable time source if `CursorBlinker` uses wall-clock time —
  inject a `std::function<steady_clock::time_point()>` for testability.
- [ ] Build and run: `ctest -R cursor_blinker_dpi`.
- [ ] Run `clang-format`.

## Acceptance Criteria

- [ ] Test exists and all three cases pass.
- [ ] No spurious transitions under DPI change.
- [ ] Time source is injectable (or CursorBlinker already supports this).

## Affected Files

- `tests/test_cursor_blinker_dpi.cpp` (new)
- `tests/CMakeLists.txt`, `tests/test_main.cpp`
- `CursorBlinker` header/source (may need injectable time source)

## Interdependencies

- Independent of other new items.
- Easier to add with `12 test-auto-registration` done first.

---

*Generated by: claude-sonnet-4-6 (consensus review 2026-03-19)*
