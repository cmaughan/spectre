---
# Megacity Demo Removal from Core Libraries

## Summary
`draxul-megacity` adds `I3DPassProvider` virtual slots to `IRenderer`, `dynamic_cast<I3DPassProvider*>` in `RendererBundle` and `HostManager`, a separate shader pair, and ~300 lines — for a rotating cube demo never used in production. All three review agents agree this should be removed from core.

## Context
`libs/draxul-megacity/`, `libs/draxul-renderer/include/draxul/renderer.h` for `I3DPassProvider`, `app/host_manager.cpp` for the `dynamic_cast`. The demo ships in the production binary and pollutes the IRenderer interface with a virtual method only one demo uses.

## Steps
- [ ] Create `demos/megacity/` directory at repo root
- [ ] Move `libs/draxul-megacity/` contents to `demos/megacity/`
- [ ] Update `CMakeLists.txt` — megacity becomes an optional top-level demo, not a library
- [ ] Remove `I3DPassProvider` from `IRenderer` hierarchy in `renderer.h`
- [ ] Remove `dynamic_cast<I3DPassProvider*>` from `RendererBundle` and `HostManager`
- [ ] Remove megacity shaders from the core shader compilation pipeline; add them to the demo's own CMakeLists
- [ ] Verify the app builds and runs without `DRAXUL_ENABLE_MEGACITY`
- [ ] Update `CLAUDE.md` architecture diagram to remove megacity from `libs/`

## Acceptance Criteria
- [ ] `libs/` contains no megacity code
- [ ] `IRenderer` hierarchy has no `I3DPassProvider`
- [ ] App builds and smoke test passes
- [ ] Demo still builds when `DRAXUL_ENABLE_MEGACITY=ON`

## Notes
**Highest-priority refactor.** Unblocks GpuCell packing. All three agents agree. Do this before renderer interface changes.

*Work item generated by claude-sonnet-4-6*
