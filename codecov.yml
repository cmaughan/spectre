coverage:
  status:
    project:
      default:
        target: auto      # compare against base commit
        threshold: 2%     # allow up to 2% drop before failing
    patch:
      default:
        target: 50%       # new code in a PR should be at least 50% covered

ignore:
  - "build/**"
  - "tests/**"
